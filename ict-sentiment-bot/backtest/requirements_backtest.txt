pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
yfinance>=0.2.36
pandas_datareader>=0.10.0
requests>=2.31.0
openpyxl>=3.1.0

# Optional — for faster/more reliable OHLCV data from your Vantage account:
# MetaTrader5>=5.0.45   (Windows only, requires MT5 terminal to be open)

# Optional — only needed if myfxbook uses full JS rendering:
# selenium>=4.18.0
